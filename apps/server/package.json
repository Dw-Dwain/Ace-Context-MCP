{
  "name": "@ace/server",
  "version": "0.0.1",
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@ace/core": "workspace:*",
    "@ace/embeddings": "workspace:*",
    "@ace/observe": "workspace:*",
    "@ace/store": "workspace:*",
    "fastify": "^5.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.5"
  },
  "license": "Apache-2.0",
  "description": "Fastify REST server + live dashboard for the context store",
  "author": "Dwain"
}
